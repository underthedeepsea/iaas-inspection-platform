# Agent Collaboration

- 遇到需要修改产品代码、修复缺陷或补充测试的任务，优先交由独立子代理执行。
- 子代理负责限定范围内的代码改动、测试和结果说明；不同子代理的写入范围必须互不重叠。
- 主会话只负责协调、查看子代理结果、集成提交，以及 GitHub 的推送、PR、Release、tag 和合并操作。
- 主会话不应与正在执行同一修复范围的子代理并行修改业务代码。
- 子代理完成后，主会话必须核对变更范围与验证证据，再进行发布操作。
