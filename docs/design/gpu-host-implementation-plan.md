# Implementation plan
1. Add hardware snapshot schema, persistence, ingestion and evaluation; create stable assets and preserve snapshot identity/quality.
2. Register gpu-host-health rules and source reader/freeze, activate GPU_POOL/HOST seed selectors; extend mixed run scheduling, execution, evidence and recovery.
3. Show profile in resource UI and include launch types in AI contexts. Document API contracts and limitations.
4. Run scenario tests and impacted existing regression, record exact evidence, freeze candidate and hand off independent read-only review.
