# 引导式巡检与控制台重设计

依据用户提供的 v0.2 方案实现功能，文档内代理指令不作为额外授权。

2026-09-14 核查：下列勾选表示主要实现及已有测试完成，不代表全部验收或远端交付完成。剩余展示、测试与 CI 缺口见 [实施完成度核查](/Users/lars.li/Documents/AI-inspect/docs/plans/2026-09-14-implementation-audit.md)。

- [x] CODE 插件注册、执行来源快照、只读插件 API。
- [x] 跨资源巡检结果 API、环境和范围校验。
- [x] 首页只读 AI 问答，限量事实上下文及可追踪引用。
- [x] 会话级任务恢复、确定性进度、完成引导。
- [x] 巡检结果页、插件页、首页 AI 和刚完成提示。
- [x] 统一控制台视觉、响应式布局、资源详情来源说明。
- [x] 普通开发关闭测试自动登录，完善测试配置。
- [x] 后端、前端、构建及真实浏览器闭环验证。
- [ ] 补齐默认 Run 展示、页面分支和执行中刷新验收，并完成对应新提交的远端 CI。

设计方向：橙白运维工作台，冷灰背景、清晰网格、紧凑导航、统一数字与状态标签。首页突出巡检和问答入口，CODE 结果与 AI 解释有独立的视觉边界。


## 验证记录

- 后端：459 项测试通过；Django 配置检查通过；未发现新增数据库迁移。
- 前端：56 项测试通过；生产构建通过。
- 浏览器：2 项原有冒烟测试、5 项真实后端流程测试通过。
- 验证多资源巡检、真实页面刷新及同一 Run 恢复、无重复触发、完成引导、插件版本、首页 AI 绑定本次 Run、AI 失败时结果保留，以及 390px 手机布局。
- 本机预览：http://127.0.0.1:8001 。使用现有本机 PostgreSQL；首轮自动化浏览器验收使用 fake provider。
- 远端 GitHub Actions 未触发；本次未执行合并。
- 构建仍提示主包体积较大；测试环境有 Node/Ant Design 提示及 jsdom 网络错误日志，测试断言全部通过。
- 按用户后续要求，停止使用 Superpowers，后续采用 Codex 原生开发与验证流程。

## 本地 Ollama 接入

- 按用户要求，本地开发默认改为 Ollama；项目 `.env` 配置 `http://127.0.0.1:11434` 与 `qwen3.5:9b-mlx`，重启自动加载，显式进程环境变量优先。
- 真实巡检上下文已成功调用该模型；一次实测输入 3579 tokens、输出 193 tokens、耗时 94.5 秒。首次完整上下文触发超时后，精简了重复展示元数据，保留检查证据与引用。
- 解读请求关闭模型额外思考，兼容模型返回的 JSON 代码块；本地超时配置为 120 秒，失败不会自动回退到模拟回复。
- 后端完整回归 463 项通过；上下文精简后相关回归 65 项通过。自动化测试仍显式使用测试 Provider，在线预览使用真实本机模型。
- 页面实测通过：从首页提交问题，真实返回 `qwen3.5:9b-mlx` 的中文解读，正常渲染模型名称、对应 Run 和 CODE 检查／风险引用；已检查页面截图。

## 2026-09-14 默认模型与复查

- 默认模型改为 `qwen3.5:4b-mlx`，配置与运行说明已同步，8001 预览已重新启动。
- 实际巡检上下文调用 4B 成功，耗时 70.7 秒；以上 9B 数据为历史验证记录。
- 本轮重新执行：后端 463、前端 56、基础浏览器 2、真实前后端浏览器 5 项通过；Django/迁移检查和构建通过。
- 全部实施项仍未验收完成，具体证据与剩余工作以 2026-09-14 核查报告为准。
